# Data Agent 工作约定

- 数据分析任务优先使用仓库技能 `data-agent`。
- 先识别完整分析、取数、探索、报表、SQL订正或知识检索模式；可用 `project stage` 从已有项目断点继续。
- 不猜测指标口径、表名、字段名或枚举值；先检索 `knowledge/`，证据不足时明确说明。
- Concept 命中后继续核对其来源 Tableau 报表和 Hive 表；生成 SQL 前检索相关 lessons。
- 生成 SQL 后，必须先运行只读安全校验，再允许执行。
- 查询结果必须保存到当前项目的 `data/` 目录；不得覆盖用户已有文件。
- 查询后运行 `data check`，阶段交付前运行对应 scope 的 `audit check`。
- 报告中的数字必须能追溯到 CSV。交付前必须运行项目审计。
- 不读取或输出 `.env` 中的令牌。不得访问名称包含 `finance`、`hr`、`salary` 的专属数据源。
- 任何写数据库、删除知识文档或外部发布行为都需要用户另行明确授权。
